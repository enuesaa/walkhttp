import { Box } from '@radix-ui/themes'
import styles from './Histories.css'

export const Histories = () => {
  return (
    <Box grow='0' shrink='0' className={styles.main}>
      histories
    </Box>
  )
}
